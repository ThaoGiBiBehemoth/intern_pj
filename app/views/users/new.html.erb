<%provide(:title, "Signup")%>
<% provide(:button_text, 'Signup!') %>
<h1>Sign up</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <%# binding.pry%>
    <p>You already have an account? &#x261E; <%= link_to "Sign in now!", signin_path %></p>
  </div>
</div>