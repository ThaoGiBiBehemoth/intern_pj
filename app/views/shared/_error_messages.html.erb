<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(object.errors.count, "error") %>..  <%# đơn giản pluralize là tạo số nhiều cho từ, cụ thể đây là 1 error, 2 errors %>
    </div>
    <ul>
      <% object.errors.full_messages.each do |mess| %>
        <li><%= mess %></li>
      <% end %>
    </ul>
  </div>
<% else %>
<% end %>